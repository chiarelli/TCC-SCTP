export enum Statuses {
    active = 'active',
    inactive = 'inactive'
}

export enum UserTypes {
    admin = 'admin',
    workspace = 'workspace',
    consumer = 'consumer',
}

export enum EvaluateStatuses {
    approved= 'approved',
    disapproved= 'disapproved',
    none= 'none',
}